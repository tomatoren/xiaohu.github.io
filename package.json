{
  "name": "personal-site",
  "version": "1.0.0",
  "private": true,
  "description": "零依赖纯静态个人生活记录站（Markdown + GitHub Pages）",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx --yes serve dist"
  },
  "license": "MIT"
}
